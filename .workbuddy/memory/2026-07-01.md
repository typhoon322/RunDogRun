## 2026-07-01 工作日志

### GH Actions Pipeline 兼容性修复 (2轮)
- **根因**: `data/provider_factory.py` L331 `os.environ.get("GITHUB_ACTIONS")` 但顶部缺少 `import os`
- 远程分支新增 EastMoneyProvider/SinaProvider/TencentProvider，ProviderFactory.__init__ 中用 os.environ 判断跳过 AKShare，但忘记 import os
- `get_factory()` 创建单例时抛 NameError → `_factory` 未赋值 → 每次调用都重复失败 → 每只股票都报 ❌
- **修复 commit 4db6f6d**: 添加 `import os` 到模块顶部
- 辅助修复 (9887fec): Python 3.11→3.10, 防御性 os import, 依赖锁定

### 数据同步性能优化 (commit f018ce7)
- **问题**: 三重嵌套重试导致每只股票最坏 360s, 60只 = 6小时
  - sync_stock(retries=2) × factory.fetch_history(retries=1) × 4 providers × 15s timeout
- **修复**:
  - sync_stock retries: 2→0 (factory 内部已做 failover)
  - factory retries: 1→0 (每源只试1次)
  - HTTP timeout: 15s→8s
  - 熔断器: provider 连续失败3次自动跳过
  - 4线程并发同步 (ThreadPoolExecutor)
  - GH Actions 跳过 baostock (login/logout 太慢)
  - pipeline max_new: 30→60
- **效果**: 3只股票 1.3s (之前需要 5+ 分钟), 60只预计 2-4分钟

### 之前累积修复
- `src/utils/logger.py`: 改为显式东八区时间
- `v2_final/analysis/drift.py`: 补 `import os`
- `v2_final/data/provider.py`: API 调用加 3 次重试
- 飞书通知已集成到每日 Pipeline 自动化

### 每日 Pipeline 自动化执行 (00:15 触发)
- v2.5-final 6 步轻量流程：5/6 通过，06_pipeline 失败（05_fetch 返回 0 stocks）
- 主体 3 分 36 秒完成；后续因 `| tail -150` 缓冲延迟退出，已 kill 进程
- 飞书消息已发送至 Yan.X (msg_id om_x100b6b0af52f3ca0c05230391157234)
- 异常：akshare/东财数据源失败，今日未刷新 daily_report/portfolio_analysis
- 改进方向：去掉 `| tail` 缓冲；fetch 失败时回退本地缓存

### 30天数据预热模式 (commit a04df7f)
- Pipeline 新增 `--data-only` 标志, 自动检测 `min_days < 30` 进入预热模式
- 预热模式只跑 ①Registry ②Universe ③Sync, 跳过策略计算
- 数据同步优化: 4线程并发 + 熔断器, 31秒完成 (之前数小时)
- Dashboard (app.py) 预热面板: 进度条 + 数据覆盖率指标, 隐藏策略板块
- min_days >= 30 后自动恢复完整 Pipeline + Dashboard
- 当前状态: min_days=3/30, 预计还需 27 个交易日
- 飞书通知已更新: 预热模式/完整模式两种消息格式
- Thread-safe singleton (threading.Lock) for provider_factory

### Streamlit Cloud NameError 修复 (commit 800b1d4)
- **问题**: app.py line 71 使用 `daily_report` 但其定义在 line 152 才出现
- 预热模式检测块引用了尚未定义的变量, 导致 NameError 崩溃
- **修复**: 将 `daily_report = _read_json("daily_report.json")` 移到预热检测之前, 移除后面的重复定义

### 3阶段智能预热 Smart Warm-up (commit 0a9075a)
- 用户反馈: 30天预热合理, 但应利用这30天"建立统计结构", 不应完全静默
- Pipeline v2.6 重构为3阶段:
  - Phase 1 COLD_START (min_days<10): 仅数据收集 ①②③
  - Phase 2 WARMUP_STAT (10≤min<30): 评分+信号记录+IC/分桶统计, 不交易
  - Phase 3 FULL (min_days≥30): 完整闭环
- Phase 2 运行: ④市场状态 ⑤数据获取 ⑥⑦⑧评分 ⑧b信号记录 ⑮前瞻收益 ⑯IC+分桶
- Phase 2 跳过: 回测/决策引擎/仓位/快照/系统健康/飞书推送
- `_build_warmup_stats()`: score分布(4桶) + 因子统计(mean/std/min/max) + Top10 + IC摘要 + 分桶
- Dashboard Phase 2 统计看板: Score分布柱状图 + 因子统计表 + IC衰减 + 分桶收益 + Top10
- 飞书通知: 3种消息格式 (Phase 1/2/3)
- 向后兼容旧 DATA_ONLY 状态
- 当前状态: Phase 1, min_days=3/10, 预计7个交易日后进入Phase 2

### V3 FINAL 生命周期状态机 (commit 8a5917e)
- **核心升级**: 从固定3阶段预热升级为统计驱动生命周期系统
- **新增模块**:
  - `core/state_machine.py`: 四阶段状态机 (COLLECT_ONLY→WARM_UP→ACTIVE→MONITORING)
    - 状态转换由统计指标驱动 (min_days/signal_count/IC/score_stability)
    - 状态持久化到 state/lifecycle.json
    - 降级回滚: IC<0 或胜率突降 → ACTIVE→WARM_UP
  - `execution/decision_engine.py` 升级: 5种输出 (EXIT/REDUCE/NO_TRADE/BUY_SMALL/BUY_FULL)
    - 每个动作包含 allow_trade/allow_hold/position_change
    - 优先从 config/params.yaml 读取阈值, 回退到 constitution.json
  - `stats/stability.py`: Score 稳定性计算 (mean_drift<5% + std_drift<10% → 稳定)
  - `report/monitor.py`: 防失效监控 (Score漂移/IC崩坏/胜率突降, 每7天检查)
    - 触发降级: IC<0 OR win_rate drop>15%
  - `config/params.yaml`: 统一参数配置 (state_machine/decision_engine/position/cooldown/monthly_lock)
- **修改模块**:
  - `pipeline/run_pipeline.py`: 行为由 state_machine 决定, 集成 monitoring 检查和降级逻辑
  - `core/feishu_sender.py`: 卡片格式升级, 增加 STATE/IC/Risk 显示
  - `app.py`: Dashboard 展示状态机状态卡片 + 状态转换历史
  - `CONSTITUTION.md`: 新增状态机/决策矩阵/降级规则
  - `README.md`: 更新为 V3 FINAL 概述
- 系统当前状态: COLLECT_ONLY, min_days 约3天

### 项目状态诊断 (用户问"对当前项目看法")
**核心结论: 架构理念好, 但运行状态多处不一致, 统计基础太薄**
关键问题 (待跟进):
1. **三套代码并存**: src/v1~v12 (86文件) + v2_final/ (42文件) + 根目录V3模块, 输出目录被混写 → 违反封版"不得改为研究平台"
2. **状态机与产出脱节**: lifecycle=COLLECT_ONLY 但 output/daily_report.md 仍是 v2.8 格式带5只持仓+收益; system_state.json 又是 regime/resonance 体系 (src/v12) → 三份文件讲三个故事
3. **统计基础薄弱**: ic_report 仅6样本, IC=0.216 p=0.681 不显著却被当"有效"; WARM_UP→ACTIVE 门槛 ic_min=0.0 太松 (IC=0.001也能过)
4. **健康评分自欺**: system_health 100/100 满分, 但维度只查数据/管线/回测/波动, 无策略有效性; 同期累计收益-1.61% 回撤20% 胜率50%
建议优先级: 系统归一 (归档src历史) > 状态机真正接管产出 > 收紧WARM_UP门槛 (IC显著+样本≥30) > 健康评分加策略有效性维度
